// The contact form no longer submits (see components/contact/contact-form.tsx),
// but this limit is still shown in the message field's character counter.
export const CONTACT_MESSAGE_MAX_LENGTH = 2000
